wrapper classes intro -- all are present in java.lang package.

DATA TYPES(8)		WRAPPER CLASSES			CONSTRUCTORS
-------------------------------------------------------------
byte				Byte					byte,String	
int					Integer					Integer,String
short				Short					Short,String
double				Double					Double,String
float				Float					Float,Double,String
long				Long					Long,String
char				Character				Character
boolean				Boolean					Boolean,String

eg: Integer i=new Integer(12);
	Integer i=new Integer("12"); // it converts int into string but not
	Integer i =new Integer("one"); 
	// but not string to int..no need to like the rest wrapper classes too
	NumberFormatException generates
	-----------------------------------
* All java wrapper classes are immutable classes,means dont allow modifications.
eg: String is immutable;
	StringBuffer is mutable	